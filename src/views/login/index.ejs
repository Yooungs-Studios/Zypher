<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login y Registro - KinglyFenix Studios</title>
    <link rel="icon" href="/esaw/vid-user-logo-defaul/zypherr(1).png">

    <style>
        /* General */
        body {
            background: linear-gradient(135deg, #0d1b2a, #1b263b);
            color: #d1d1d1;
            font-family: 'Roboto', sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }

        /* Contenedor Principal */
        .container {
            display: flex;
            background-color: rgba(20, 20, 40, 0.95);
            border-radius: 20px;
            box-shadow: 0 15px 40px rgba(0, 0, 0, 0.8);
            overflow: hidden;
            width: 900px;
            max-width: 100%;
        }

        /* Sección Izquierda con Imagen */
        .image-section {
            position: relative;
            width: 45%;
            background: url('/esaw/vid-user-logo-defaul/logo.png') center/cover;
            display: flex;
            justify-content: center;
            align-items: center;
            text-align: center;
            flex-direction: column;
            padding: 20px;
        }

        .image-section::before {
            content: '';
            position: absolute;
            inset: 0;
            background: rgba(0, 0, 40, 0.6);
            backdrop-filter: blur(5px);
        }

        .image-section h2 {
            position: relative;
            color: #e0e0e0;
            font-size: 2.5rem;
            padding-bottom: 10px;
        }

        .image-section .phrase {
            position: relative;
            color: #60a5fa;
            font-size: 1rem;
            font-style: italic;
        }

        /* Formulario Container */
        .form-container {
            width: 55%;
            padding: 50px;
            display: flex;
            flex-direction: column;
            justify-content: center;
            animation: fadeIn 1s ease-out;
        }

        /* Animación de fadeIn */
        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateX(20px);
            }
            to {
                opacity: 1;
                transform: translateX(0);
            }
        }

        /* Logo */
        .logo {
            font-size: 2.5em;
            color: #60a5fa;
            margin-bottom: 20px;
            letter-spacing: 2px;
            font-weight: 700;
            text-align: center;
        }

        /* Frase dinámica */
        .dynamic-phrase {
            color: #9ca3af;
            font-size: 1rem;
            text-align: center;
            margin-bottom: 25px;
            font-style: italic;
        }

        /* Botones de Alternar */
        .toggle-buttons {
            display: flex;
            justify-content: center;
            margin-bottom: 25px;
        }

        .toggle-btn {
            background: transparent;
            border: 2px solid #60a5fa;
            padding: 10px 30px;
            border-radius: 25px;
            color: #60a5fa;
            cursor: pointer;
            margin: 0 10px;
            transition: all 0.3s ease;
            font-weight: 500;
        }

        .toggle-btn.active,
        .toggle-btn:hover {
            background-color: #60a5fa;
            color: #121212;
        }

        /* Formularios */
        .form {
            display: none;
            text-align: left;
            animation: slideIn 0.5s ease-out;
        }

        .form.active {
            display: block;
        }

        /* Animación de slideIn */
        @keyframes slideIn {
            from {
                opacity: 0;
                transform: translateX(30px);
            }
            to {
                opacity: 1;
                transform: translateX(0);
            }
        }

        /* Grupo de Entrada */
        .input-group {
            margin-bottom: 20px;
        }

        .input-group label {
            display: block;
            margin-bottom: 8px;
            color: #b3b3b3;
            font-weight: 500;
        }

        .input-group input {
            width: 100%;
            padding: 12px;
            background-color: #1a1a2e;
            border: 1px solid #3a4f6c;
            border-radius: 8px;
            color: #e0e0e0;
            transition: border-color 0.3s ease;
        }

        .input-group input:focus {
            outline: none;
            border-color: #60a5fa;
            box-shadow: 0 0 5px rgba(96, 165, 250, 0.5);
        }

        /* Botón Primario */
        .btn-primary {
            width: 100%;
            padding: 14px;
            background-color: #60a5fa;
            border: none;
            border-radius: 25px;
            color: #121212;
            font-weight: 600;
            cursor: pointer;
            transition: background-color 0.3s ease;
            box-shadow: 0 4px 15px rgba(96, 165, 250, 0.3);
        }

        .btn-primary:hover {
            background-color: #3b82f6;
        }

        /* Mensajes de Error */
        .error-message {
            color: #ff5c5c;
            margin-top: 15px;
            font-size: 0.9em;
            text-align: center;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="image-section">
            <h2>Bienvenido a <br><strong>Zypher</strong></h2>
            <p id="phrase" class="phrase">"¡Vamos a hacer algo épico!"</p>
        </div>

        <div class="form-container">
            <h1 class="logo">Zypher</h1>
            <p id="dynamic-phrase" class="dynamic-phrase">Inicia sesión para continuar.</p>

            <!-- Botones para cambiar entre Login y Registro -->
            <div class="toggle-buttons">
                <button id="show-login" class="toggle-btn active">Login</button>
                <button id="show-register" class="toggle-btn">Registro</button>
            </div>

            <!-- Formulario de Login -->
            <form id="login-form" class="form active" action="/auth/loginc" method="post">
                <div class="input-group">
                    <label for="login-user">Correo</label>
                    <input type="email" id="login-user"  name="username" placeholder="Tu usuario o correo">
                </div>
                <div class="input-group">
                    <label for="login-password">Contraseña</label>
                    <input type="password" id="login-password"  name="password" placeholder="Tu contraseña">
                </div>
                <button type="submit" class="btn-primary">Iniciar Sesión</button>
            </form>

            <!-- Formulario de Registro -->
            <form id="register-form" class="form" action="/auth/register" method="post">
                <div class="input-group">
                    <label for="register-user">Nombre</label>
                    <input type="text" id="register-user"  name="fullname" placeholder="juan paco mendez tercero">
                </div>
                <!-- <div class="input-group">
                    <label for="register-user">Nombre de Usuario</label>
                    <input type="text" id="register-user"  name="username" placeholder="KinglyShade">
                </div> -->
                <div class="input-group">
                    <label for="register-email">Correo Electrónico</label>
                    <input type="email" id="register-email"  name="email" placeholder="ejemplo@correo.com">
                </div>
                <div class="input-group">
                    <label for="register-password">Contraseña</label>
                    <input type="password" name="password" id="register-password" placeholder="Tu contraseña">
                </div>
                <div class="input-group">
                    <label for="confirm-password">Repite la Contraseña</label>
                    <input type="password" name="confirmpassword" id="confirm-password" placeholder="Repite tu contraseña">
                </div>
                <div class="input-group">
                    <label for="pais">País</label>
                    <input type="text" id="pais" name="pais" placeholder="Tu país">
                </div>
                <button type="submit" class="btn-primary">Registrarse</button>
            </form>
        </div>
    </div>

    <script>
        const loginForm = document.getElementById('login-form');
        const registerForm = document.getElementById('register-form');
        const dynamicPhrase = document.getElementById('dynamic-phrase');
        const toggleButtons = document.querySelectorAll('.toggle-btn');

        toggleButtons.forEach(button => {
            button.addEventListener('click', () => {
                toggleButtons.forEach(btn => btn.classList.remove('active'));
                button.classList.add('active');

                if (button.id === 'show-login') {
                    loginForm.classList.add('active');
                    registerForm.classList.remove('active');
                    dynamicPhrase.textContent = 'Inicia sesión para continuar.';
                } else {
                    registerForm.classList.add('active');
                    loginForm.classList.remove('active');
                    dynamicPhrase.textContent = 'Crea una cuenta para unirte a la diversión.';
                }
            });
        });
    </script>
</body>
</html>

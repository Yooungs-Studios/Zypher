<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Restablecer Contraseña - KinglyFenix Studios</title>
    <style>
        /* General */
        body {
            background: linear-gradient(135deg, #0d1b2a, #1b263b);
            color: #d1d1d1;
            font-family: 'Roboto', sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }

        /* Contenedor Principal */
        .container {
            display: flex;
            background-color: rgba(20, 20, 40, 0.95);
            border-radius: 20px;
            box-shadow: 0 15px 40px rgba(0, 0, 0, 0.8);
            overflow: hidden;
            width: 600px;
            max-width: 100%;
            flex-direction: column;
            align-items: center;
            padding: 40px 20px;
        }

        /* Logo */
        .logo {
            font-size: 2.5em;
            color: #60a5fa;
            margin-bottom: 20px;
            letter-spacing: 2px;
            font-weight: 700;
            text-align: center;
        }

        /* Mensaje */
        .message {
            color: #9ca3af;
            font-size: 1rem;
            text-align: center;
            margin-bottom: 25px;
            font-style: italic;
        }

        /* Formulario */
        .form {
            width: 100%;
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        .input-group {
            margin-bottom: 20px;
            width: 100%;
        }

        .input-group label {
            display: block;
            margin-bottom: 8px;
            color: #b3b3b3;
            font-weight: 500;
        }

        .input-group input {
            width: 100%;
            padding: 12px;
            background-color: #1a1a2e;
            border: 1px solid #3a4f6c;
            border-radius: 8px;
            color: #e0e0e0;
            transition: border-color 0.3s ease;
        }

        .input-group input:focus {
            outline: none;
            border-color: #60a5fa;
            box-shadow: 0 0 5px rgba(96, 165, 250, 0.5);
        }

        .btn-primary {
            width: 100%;
            padding: 14px;
            background-color: #60a5fa;
            border: none;
            border-radius: 25px;
            color: #121212;
            font-weight: 600;
            cursor: pointer;
            transition: background-color 0.3s ease;
            box-shadow: 0 4px 15px rgba(96, 165, 250, 0.3);
        }

        .btn-primary:hover {
            background-color: #3b82f6;
        }

        .error-message {
            color: #ff5c5c;
            margin-top: 15px;
            font-size: 0.9em;
            text-align: center;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1 class="logo">Zypher</h1>
        <p class="message">Ingresa tu nueva contraseña</p>

        <!-- Formulario de Reset de Contraseña -->
        <form action="/auth/resetp" method="POST" class="form">
            <!-- Si hay un error en el token -->
            <% if (error) { %>
                <div class="error-message"><%= error %></div>
            <% } %>

            <!-- Campo de Nueva Contraseña -->
            <div class="input-group">
                <label for="new-password">Nueva Contraseña</label>
                <input type="password" id="new-password" name="new-password" placeholder="Tu nueva contraseña" required>
            </div>

            <!-- Campo de Confirmación de Contraseña -->
            <div class="input-group">
                <label for="confirm-password">Repite la Contraseña</label>
                <input type="password" id="confirm-password" name="confirm-password" placeholder="Repite tu contraseña" required>
            </div>

            <!-- Campo para el Token -->
            <input type="hidden" name="token" value="<%= token %>">

            <!-- Botón para enviar el formulario -->
            <button type="submit" class="btn-primary">Restablecer Contraseña</button>
        </form>
    </div>
</body>
</html>

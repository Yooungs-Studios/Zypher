<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Verificación de Correo - KinglyFenix Studios</title>
    <style>
         body {
            background: linear-gradient(135deg, #0d1b2a, #1b263b);
            color: #d1d1d1;
            font-family: 'Roboto', sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }

        /* Contenedor Principal */
        .container {
            display: flex;
            background-color: rgba(20, 20, 40, 0.95);
            border-radius: 20px;
            box-shadow: 0 15px 40px rgba(0, 0, 0, 0.8);
            overflow: hidden;
            width: 900px;
            max-width: 100%;
        }
        /* Sección Izquierda con Imagen */
        .image-section {
            position: relative;
            width: 45%;
            background: url('/esaw/vid-user-logo-defaul/logo.png') center/cover;
            display: flex;
            justify-content: center;
            align-items: center;
            text-align: center;
            flex-direction: column;
            padding: 20px;
        }

        .image-section::before {
            content: '';
            position: absolute;
            inset: 0;
            background: rgba(0, 0, 40, 0.6);
            backdrop-filter: blur(5px);
        }

        .image-section h2 {
            position: relative;
            color: #e0e0e0;
            font-size: 2.5rem;
            padding-bottom: 10px;
        }

        .image-section .phrase {
            position: relative;
            color: #60a5fa;
            font-size: 1rem;
            font-style: italic;
        }

        /* Formulario Container */
        .form-container {
            width: 50%;
            display: flex;
            flex-direction: column;
            justify-content: center;
            padding: 30px;
        }

        /* Título Principal */
        .logo {
            font-size: 2.5em;
            color: #60a5fa;
            margin-bottom: 20px;
            letter-spacing: 2px;
            font-weight: 700;
            text-align: center;

        }

        .dynamic-phrase {
            color: #9ca3af;
            font-size: 1rem;
            text-align: center;
            margin-bottom: 25px;
            font-style: italic;
        }

        /* Mensajes de Estado */
        .status-message {
            text-align: center;
            margin-top: 20px;
            font-size: 1.1em;
            font-weight: 500;
        }

        .status-message.success {
            color: #60a5fa;
        }

        .status-message.error {
            color: #ff5c5c;
        }

        /* Botón de Regreso */
        .back-btn {
            width: 100%;
            padding: 14px;
            background-color: #60a5fa;
            border: none;
            border-radius: 25px;
            color: #121212;
            font-weight: 600;
            cursor: pointer;
            margin-top: 20px;
            transition: background-color 0.3s ease;
        }

        .back-btn:hover {
            background-color: #3b82f6;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="image-section">
            <h2>Bienvenido a <br><strong>Zypher</strong></h2>
            <p id="phrase" class="phrase">"¡Haci inicia una gran historia!"</p>
        </div>


        <div class="form-container">
            <h1 class="logo">Verificación de Correo</h1>
            <p class="dynamic-phrase">Verifica tu correo para continuar.</p>

            <!-- Mensaje de éxito o error -->
            <div class="status-message <%= status %>">
                <%= message %>
            </div>

            <!-- Botón de Regreso -->
            <form action="/auth/login" method="get">
                <button type="submit" class="back-btn">Volver al Login</button>
            </form>
        </div>
    </div>
</body>
</html>

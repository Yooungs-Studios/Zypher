<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Registro de Creador</title>
    <style>
        body {
            font-family: 'Poppins', sans-serif;
            margin: 0;
            padding: 0;
            background: linear-gradient(90deg, #0e192c, #303030);

            color: #fff;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
        }

        .form-container {
             margin: 10px;
            background: linear-gradient(90deg, #0e192c, #100457);
            border-radius: 12px;
            padding: 30px;
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.5);
            width: 100%;
            max-width: 500px;
        }

        h1 {
            margin-bottom: 20px;
            font-size: 1.8em;
            color: #63b4f6;
            text-align: center;
        }

        .steps {
            display: flex;
            justify-content: space-between;
            margin-bottom: 20px;
        }

        .step {
            flex: 1;
            padding: 10px 0;
            text-align: center;
            background: #12263f;
            border-radius: 8px;
            margin: 0 5px;
            font-size: 0.85em;
            font-weight: 500;
            transition: background-color 0.3s ease;
        }

        .step.active {
            background: #045394b9;
        }

        .form-step {
            display: none;
            flex-direction: column;
        }

        .form-step.active {
            display: flex;
        }

        label {
            margin-top: 10px;
            font-size: 0.95em;
            text-align: left;
            width: 100%;
            margin-bottom: 5px;
        }

        input, textarea, select {
            width: 100%;
            padding: 12px;
            border-radius: 8px;
            border: none;
            background: #12263f;
            color: #fff;
            margin-bottom: 15px;
            font-size: 0.95em;
        }

        textarea {
            resize: none;
            height: 100px;
        }

        button {
            background: linear-gradient(90deg, #50ac81, #053068);
            color: #fff;
            padding: 12px;
            border: none;
            border-radius: 8px;
            font-size: 1em;
            font-weight: 500;
            cursor: pointer;
            transition: background-color 0.3s ease;
            margin: 5px;
        }

        button:hover {
            background: #63b4f6;
        }

        button:disabled {
            background: #1c3d5a;
            cursor: not-allowed;
        }

       
        img {
            display: block;
            max-width: 100%;
            height: auto;
            margin: 10px auto;
            border: 2px solid #fff;
        }
        .avatar{
            border-radius: 200px;
            max-width: 30%;
            max-height: 30%;

        }
        .form-container img.banner {
            max-height: 200px;
        }
        .alert {
    padding: 15px;
    margin: 20px 0;
    border-radius: 8px;
    font-size: 1.1em;
    display: flex;
    align-items: center;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.3);
    opacity: 0;
    transform: translateY(-10px);
    animation: fadeInAlert 0.5s forwards;
}

.alert i {
    margin-right: 10px;
    font-size: 1.5em;
}

.alert.error {
    background-color: #e74c3c;
    color: #fff;
    border-left: 5px solid #c0392b;
}

.alert.success {
    background-color: #27ae60;
    color: #fff;
    border-left: 5px solid #229954;
}

.alert.info {
    background-color: #3498db;
    color: #fff;
    border-left: 5px solid #2980b9;
}
    </style>
      <link rel="icon" href="/esaw/vid-user-logo-defaul/zypherr(1).png">

</head>
<body>
    <div class="form-container">
        <h1>Registro de Creador</h1>
        <div class="steps">
            <span class="step active">Cuenta</span>
            <span class="step">Perfil</span>
            <span class="step">Redes</span>
        </div>

        <form id="uploadForm" action="/guardarCreador" method="POST"  enctype="multipart/form-data">
            <!-- Paso 1: Datos de la Cuenta -->
            <div class="form-step active">
                <label for="nickname">Nickname</label>
                <input type="text" id="nickname" name="nickname" placeholder="Nickname único" required>
                <label for="nickname">Identificador</label>
                <input type="text" id="nickname" name="id" placeholder="@Juanito-95" required>

                <label for="descripcion">Descripción</label>
                <textarea id="descripcion" name="descripcion" placeholder="Escribe una breve biografía"></textarea>

                <button type="button" class="next-step">Siguiente</button>
            </div>

            <!-- Paso 2: Perfil del Creador -->
            <div class="form-step">
                <label for="avatar">Avatar</label>
                <input type="file" id="avatar" name="avatar" accept="image/*" required>
                                  <img class="avatar" id="avatarPreview" src="" alt="Vista previa del avatar">

                <label for="banner">Banner</label>
                <input type="file" id="banner" name="banner" accept="image/*" required>                <img id="bannerPreview" src="" alt="Vista previa del banner">

                <button type="button" class="prev-step">Anterior</button>
                <button type="button" class="next-step">Siguiente</button>
            </div>

            <!-- Paso 3: Redes Sociales -->
            <div class="form-step">
                <label for="redes_sociales">Redes Sociales ( temporal mente desabilitado esta opcion se pondra en funcionamiento pronto )</label>
                <textarea id="redes_sociales" name="redes_sociales" placeholder='{"twitter": "https://...", "instagram": "https://..."}'></textarea>

                <button type="button" class="prev-step">Anterior</button>
                <button type="submit">Finalizar</button>
            </div>
        </form>
    </div>

    <script>
         document.getElementById('uploadForm').addEventListener('submit', function (e) {
    e.preventDefault(); // Prevenir el comportamiento por defecto (envío inmediato)

    // Mostrar la pantalla de carga
    // document.getElementById('loadingScreen').style.display = 'flex';

    // Enviar el formulario usando AJAX para no recargar la página
    const formData = new FormData(this);

    fetch('/guardarCreador', {
        method: 'POST',
        body: formData
    })
    .then(response => response.json())
    .then(data => {
        // Cuando la carga se complete, ocultar la pantalla de carga
        // document.getElementById('loadingScreen').style.display = 'none';
        
        // Redirigir o mostrar un mensaje de éxito, dependiendo de tu lógica
        showAlert('success', 'Creador registrado correcta mente.');
        alert('Creador registrado correcta mente.')

       window.location.href = '/'; // O cualquier otra lógica de éxito
    })
    .catch(error => {
        console.error('Error durante la carga:', error);
        showAlert('error', 'Error durante la carga.');
        alert('Error durante la carga.')

    });
});
function showAlert(type, message) {
    const alertContainer = document.createElement('div');
    alertContainer.classList.add('alert', type);
    alertContainer.innerHTML = `<i>${getIcon(type)}</i> ${message}`;
    
    document.querySelector('.form-container').prepend(alertContainer);

    // Eliminar la alerta después de 5 segundos
    setTimeout(() => {
        alertContainer.remove();
    }, 5000);
}

function getIcon(type) {
    switch (type) {
        case 'error':
            return '&#9888;'; // Símbolo de advertencia
        case 'success':
            return '&#10004;'; // Símbolo de check
        case 'info':
            return '&#9432;'; // Símbolo de información
        default:
            return '';
    }
}
        const steps = document.querySelectorAll(".step");
        const formSteps = document.querySelectorAll(".form-step");
        let currentStep = 0;

        document.querySelectorAll(".next-step").forEach((btn) => {
            btn.addEventListener("click", () => {
                if (currentStep < steps.length - 1) {
                    formSteps[currentStep].classList.remove("active");
                    steps[currentStep].classList.remove("active");
                    currentStep++;
                    formSteps[currentStep].classList.add("active");
                    steps[currentStep].classList.add("active");
                }
            });
        });

        document.querySelectorAll(".prev-step").forEach((btn) => {
            btn.addEventListener("click", () => {
                if (currentStep > 0) {
                    formSteps[currentStep].classList.remove("active");
                    steps[currentStep].classList.remove("active");
                    currentStep--;
                    formSteps[currentStep].classList.add("active");
                    steps[currentStep].classList.add("active");
                }
            });
        });

        function previewImage(input, previewId) {
            const preview = document.getElementById(previewId);
            const file = input.files[0];
            if (file) {
                const url = URL.createObjectURL(file);
                preview.src = url;
            }
        }

        document.getElementById('avatar').addEventListener('change', function () {
            previewImage(this, 'avatarPreview');
        });

        document.getElementById('banner').addEventListener('change', function () {
            previewImage(this, 'bannerPreview');

        });
    </script>

</body>
</html>

<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= post.titulo %> - Zypher</title>
    <style>
        /* Global Styles */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Roboto', sans-serif;
            line-height: 1.6;
            background-color: #121212;
            color: #e0e0e0;
        }

        a {
            text-decoration: none;
            color: #1e90ff;
            transition: color 0.3s ease;
        }

        a:hover {
            color: #00d1b2;
        }

        /* Header */
        header {
            background: linear-gradient(135deg, #1e1e2e, #292938);
            color: #ffffff;
            padding: 30px;
            text-align: center;
            border-bottom: 2px solid #1e90ff;
        }

        header h1 {
            font-size: 2.5em;
            margin-bottom: 10px;
        }

        /* Navigation Menu */
        .menu {
            display: flex;
            justify-content: center;
            gap: 20px;
            background: #1e1e2e;
            padding: 15px 0;
            border-bottom: 2px solid #292938;
        }

        .menu a {
            color: #9da5b4;
            font-weight: bold;
        }

        .menu a:hover {
            color: #00d1b2;
        }

        /* Main Container */
        .container {
            display: grid;
            grid-template-columns: 3fr 1fr;
            gap: 20px;
            max-width: 1200px;
            margin: 40px auto;
            padding: 0 20px;
        }

        /* Blog Main Section */
        .blog-main {
            background: #1e1e2e;
            border-radius: 10px;
            padding: 20px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.3);
        }

        .blog-main h2 {
            font-size: 2em;
            margin-bottom: 20px;
            color: #00d1b2;
        }

        .blog-main h3 {
            font-size: 1.5em;
            margin-top: 20px;
            color: #9da5b4;
        }

        .blog-main p {
            margin: 10px 0;
            color: #b2b2b2;
        }

        /* Sidebar */
        .sidebar {
            background: #1e1e2e;
            border-radius: 10px;
            padding: 20px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.3);
        }

        .sidebar h3 {
            font-size: 1.5em;
            margin-bottom: 15px;
            color: #00d1b2;
        }

        .sidebar ul {
            list-style: none;
            padding: 0;
        }

        .sidebar ul li {
            margin-bottom: 10px;
        }

        .sidebar ul li a {
            color: #1e90ff;
        }

        /* Footer */
        footer {
            background: #1e1e2e;
            color: #9da5b4;
            text-align: center;
            padding: 20px;
            margin-top: 40px;
            border-top: 2px solid #1e90ff;
        }

        footer p {
            margin: 0;
        }

        /* Responsive Design */
        @media (max-width: 768px) {
            .container {
                grid-template-columns: 1fr;
            }

            .menu {
                flex-wrap: wrap;
                gap: 10px;
            }
        }
    </style>
</head>
<body>
        <%- include('./header.ejs') %>

    <header>
        <h1>Blog de Zypher</h1>
    </header>
    <div class="container">
        <div class="blog-main">
            <h2><%= post.titulo %></h2>
            <p><strong>Publicado el:</strong> <%= new Date(post.fecha_publicacion).toLocaleDateString() %></p>
            <p><%= post.contenido %></p>
            <h3>Autor: <%= post.autor_nombre %> - <%= post.autor_rol %></h3>
            <div>
                <img src="<%= post.autor_avatar || '/path/to/default-avatar.png' %>" alt="Avatar de <%= post.autor_nombre %>" style="width: 40px; height: 40px; border-radius: 50%;">
            </div>
            <p><a href="/blog">Volver al Blog</a></p>
        </div>

        <div class="sidebar">
            <h3>Categorías</h3>
            <ul>
                <% categorias.forEach(categoria => { %>
                    <li><a href="/blog/categoria/<%= categoria.id %>"><%= categoria.nombre %></a></li>
                <% }) %>
            </ul>
        </div>
    </div>

    <footer>
        <p>© 2024 Zypher | Todos los derechos reservados</p>
    </footer>

</body>
</html>

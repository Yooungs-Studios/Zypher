function viewCollaboration(collabId) {
    alert(`Ver detalles de la colaboración con ID: ${collabId}`);
}

function acceptCollaboration(requestId) {
    alert(`Solicitud de colaboración aceptada con ID: ${requestId}`);
}

function rejectCollaboration(requestId) {
    alert(`Solicitud de colaboración rechazada con ID: ${requestId}`);
}
